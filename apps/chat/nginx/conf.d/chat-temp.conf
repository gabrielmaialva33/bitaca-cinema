# Temporary HTTP-only configuration for Certbot
server {
    listen 80;
    listen [::]:80;
    server_name chat.abitaca.com.br;

    root /opt/bitaca-cinema/apps/chat;
    index index.html;

    # For Certbot challenge
    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
