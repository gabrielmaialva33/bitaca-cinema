# ========================================
# GALERIA BITACA CAFÉ BAR - FRONTEND
# Site sobre o espaço físico da Galeria Bitaca
# ========================================

server {
    listen 80;
    listen [::]:80;
    server_name galeria.abitaca.com.br;

    # Logs específicos para a galeria
    access_log /var/log/nginx/galeria-access.log;
    error_log /var/log/nginx/galeria-error.log;

    # Root do site da galeria
    root /opt/bitaca-cinema/apps/galeria-bitaca;
    index index.html;

    # Charset UTF-8
    charset utf-8;

    # Servir arquivos estáticos
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Assets (CSS, JS, images)
    location ~* ^/assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Imagens
    location ~* \.(jpg|jpeg|png|gif|ico|svg|webp)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # Fontes
    location ~* \.(woff|woff2|ttf|otf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Access-Control-Allow-Origin "*";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # Desabilitar cache para HTML
    location ~* \.html$ {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
    }

    # Desabilitar acesso a arquivos ocultos
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    # Favicon
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }

    # Robots.txt
    location = /robots.txt {
        log_not_found off;
        access_log off;
    }

    # Compressão gzip
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/x-javascript
        application/json
        application/xml
        application/xml+rss
        image/svg+xml;
}

# ========================================
# SSL Configuration (será configurado com Certbot)
# ========================================
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name galeria.abitaca.com.br;
#
#     ssl_certificate /etc/letsencrypt/live/galeria.abitaca.com.br/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/galeria.abitaca.com.br/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#
#     # ... (mesmo conteúdo do bloco server acima)
# }
